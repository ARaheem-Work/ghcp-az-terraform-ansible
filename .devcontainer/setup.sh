#sudo apt update
#sudo apt install -y software-properties-common
#sudo add-apt-repository --yes --update ppa:ansible/ansible # if you get error, then try editing /usr/bin/add-apt-repository, point python3 to python3.5
#sudo apt install -y ansible

#sudo apt install -y python3-pip

#ansible-galaxy collection install azure.azcollection --force
#ansible-galaxy collection install azure.azcollection:3.5.0 --force

#sudo pip3 install -r ~/.ansible/collections/ansible_collections/azure/azcollection/requirements.txt

echo "Ansible and Azure collection installed successfully."

